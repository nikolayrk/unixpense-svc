kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraMounts:
  - hostPath: $GITHUB_WORKSPACE # Evaluated in .github/workflows/helm_tests.yml
    containerPath: /usr/src
