apiVersion: v1
kind: Service
metadata:
  name: {{ include "unixpense-svc.fullname" . }}-app
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "unixpense-svc.fullname" . }}
spec:
  selector:
    app: {{ include "unixpense-svc.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP