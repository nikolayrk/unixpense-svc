apiVersion: apps/v1
kind: Deployment
metadata:
  name: unixpense-svc
  namespace: unixpense
spec:
  selector:
    matchLabels:
      app: unixpense-svc
  replicas: 1
  template:
    metadata:
      labels:
        app: unixpense-svc
    spec:
      securityContext:
        runAsUser: 1000
      containers:
      - name: app
        image: DOCKER_REPO:BUILD_NUMBER # Populated from environmental variables via Jenkins Job
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: unixpense-svc-config
              key: PORT
              optional: false
        - name: CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-googlecred
              key: CLIENT_ID
              optional: false
        - name: CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-googlecred
              key: CLIENT_SECRET
              optional: false
        - name: REDIRECT_URI
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-googlecred
              key: REDIRECT_URI
              optional: false
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-dbcred
              key: DB_HOST
              optional: false
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-dbcred
              key: DB_PORT
              optional: false
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-dbcred
              key: DB_USERNAME
              optional: false
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: unixpense-svc-dbcred
              key: DB_PASSWORD
              optional: false